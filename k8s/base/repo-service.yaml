apiVersion: apps/v1
kind: Deployment
metadata:
  name: repo-service
  namespace: chrono-vcs
  labels: { app.kubernetes.io/name: repo-service, app.kubernetes.io/component: backend }
spec:
  replicas: 2
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate: { maxSurge: 1, maxUnavailable: 0 }
  selector:
    matchLabels: { app.kubernetes.io/name: repo-service }
  template:
    metadata:
      labels:
        app.kubernetes.io/name: repo-service
        app.kubernetes.io/component: backend
    spec:
      serviceAccountName: repo-service
      automountServiceAccountToken: true
      terminationGracePeriodSeconds: 30
      # Chainguard distroless UID 65532. The EFS access point (terraform/efs.tf)
      # enforces POSIX identity UID/GID 1000 server-side: every file op is
      # rewritten, so the pod UID does not need to match. fsGroup stays 65532
      # for the local emptyDir volumes.
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        fsGroup: 65532
        seccompProfile: { type: RuntimeDefault }
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels: { app.kubernetes.io/name: repo-service }
      containers:
        - name: app
          image: repo-service
          imagePullPolicy: IfNotPresent
          ports:
            - { name: http, containerPort: 8000, protocol: TCP }
          envFrom:
            - configMapRef: { name: repo-service-config }
            - secretRef:    { name: db-app-credentials }
            - secretRef:    { name: identity-misc }
          resources:
            requests: { cpu: 150m, memory: 384Mi }
            limits:   { memory: 768Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: [ALL] }
          startupProbe:
            httpGet: { path: /ping, port: http }
            failureThreshold: 30
            periodSeconds: 2
          readinessProbe:
            httpGet: { path: /health, port: http }
            periodSeconds: 5
          livenessProbe:
            httpGet: { path: /ping, port: http }
            periodSeconds: 10
          volumeMounts:
            - { name: tmp, mountPath: /tmp }
            # App default EFS_DRAFTS_ROOT is /mnt/efs/drafts.
            - { name: drafts, mountPath: /mnt/efs/drafts }
      volumes:
        - name: tmp
          emptyDir: { sizeLimit: 128Mi }
        - name: drafts
          persistentVolumeClaim:
            claimName: repo-drafts-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: repo-service
  namespace: chrono-vcs
spec:
  type: ClusterIP
  selector: { app.kubernetes.io/name: repo-service }
  ports:
    - { name: http, port: 8000, targetPort: http }
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: repo-service
  namespace: chrono-vcs
spec:
  minAvailable: 1
  selector:
    matchLabels: { app.kubernetes.io/name: repo-service }
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: repo-service
  namespace: chrono-vcs
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: repo-service
  minReplicas: 2
  maxReplicas: 8
  metrics:
    - type: Resource
      resource:
        name: cpu
        target: { type: Utilization, averageUtilization: 70 }
